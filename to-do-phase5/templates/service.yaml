apiVersion: v1
kind: Service
metadata:
  name: {{ include "to-do-phase5.fullname" . }}
  labels:
    {{- include "to-do-phase5.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "to-do-phase5.selectorLabels" . | nindent 4 }}
