apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "to-do-phase5.fullname" . }}-test-connection"
  labels:
    {{- include "to-do-phase5.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "to-do-phase5.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
